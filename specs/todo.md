# TODO 웹 앱

할 일을 적고, 끝난 것을 체크하고, 지우는 가장 단순한 웹 앱을 만든다.
데이터는 서버 메모리에 둔다. 로그인도 데이터베이스도 없다.

## 기능

- 할 일 목록을 본다
- 할 일을 추가한다 (제목만)
- 할 일을 완료/미완료로 바꾼다
- 할 일을 지운다
- 남은 개수를 화면에 보여 준다

## 화면

한 페이지짜리다. 입력창 하나, 추가 버튼 하나, 목록, 그리고 "남은 일 N개".
꾸밈은 최소한으로 하되 글자가 읽히고 버튼이 눌리는 정도는 되어야 한다.
